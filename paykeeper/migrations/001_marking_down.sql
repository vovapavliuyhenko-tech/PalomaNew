-- ============================================================================
-- Миграция 001 — маркировка «Честный ЗНАК» (DOWN: разобрать обратно)
-- Удаляем таблицы в обратном порядке (сначала те, что ссылаются на другие).
-- ВНИМАНИЕ: это стирает ВСЕ коды, заказы и журнал. Только для отката на копии.
-- ============================================================================

DROP TABLE IF EXISTS marking_code_log;
DROP TABLE IF EXISTS order_items;
DROP TABLE IF EXISTS orders;
DROP TABLE IF EXISTS marking_codes;
